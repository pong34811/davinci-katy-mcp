# .hermes.md — Hermes-Specific Project Rules

> This file is loaded by Hermes when running in this repo. It provides
> Hermes-specific behavior (slash commands, toolsets, skill invocation)
> on top of the portable AGENTS.md.

## Skill Invocation Rules

**ก่อนเริ่มงานใดๆ ต้องเช็ค skill ก่อนเสมอ:**

| User says | Invoke |
|---|---|
| เพิ่ม SFX / ใส่เสียงประกอบ / แก้คลิป | `adding-sfx` |
| แก้/เช็ค SFX ที่วางแล้ว, "ใส่น้อย", "ตรวจละเอียด" | `sfx-review` |
| วิเคราะห์ subtitle / transcript | `subtitle-analyzer` + `sfx-story-analyzer` |
| หา/เปรียบเทียบเสียงจาก library | `sfx-library-manager` |
| timeline, color, render, export, media pool | `davinci-resolve-workflow` |
| สร้าง/แก้ skill | `skill-creator` |
| debug bug | `systematic-debugging` |
| feature ใหม่ / creative work | `brainstorming` |
| spreadsheet / csv / xlsx | `xlsx` |

## Mandatory Pre-Work Checklist

```
[ ] อ่าน Tier 1: AGENTS.md, CLAUDE.md, .opencode/agent/skill-first.md
[ ] ระบุ work type (SFX / subtitle / emotion / resolve / other)
[ ] Invoke skill ที่เหมาะสม (ถ้ามี)
[ ] เริ่มงาน
```

## Subagent for SFX

เมื่องานเกี่ยวกับ SFX ใน DaVinci Resolve ให้ spawn `SFX Editor` subagent:
- mode: subagent
- workflow: invoke `adding-sfx` → inspect timeline → analyze subtitle → write plan → dry-run → place → verify

## Toolset Requirements

Always ensure these toolsets are enabled for this project:
- `terminal` — running scripts, placing SFX
- `code_execution` — Python analysis scripts
- `file` — reading/writing project files
- `skills` — skill management
- `todo` — task planning
- `clarify` — asking user questions when needed

## Slash Commands

| Command | Use when |
|---|---|
| `/skills` | Browse/install/manage skills |
| `/curator` | Skill maintenance (status, run, pin, archive) |
| `/cron` | Schedule recurring tasks |
| `/delegate` | Spawn subagent for parallel work |
| `/goals` | Set standing goal across turns |
| `/new` | Fresh session |
| `/compress` | Compress context when token-heavy |
| `/undo` | Roll back filesystem changes |

## Key Paths

```
C:\Users\warit\Desktop\davinci-katy-mcp\
├── .opencode/skills/        — Skill definitions (SKILL.md)
├── .opencode/agent/         — Agent definitions (skill-first.md, sfx-editor.md)
├── scripts/                 — CLI tools (sfx_place.py, analyze_subtitles.py, etc.)
├── davinci-resolve-mcp/     — MCP server source
├── SFX/                     — Raw SFX library (mp3/wav)
├── scripts/plan.json        — Active SFX plan
├── scripts/subtitles_beats.json — Beat analysis from SRT file
└── AGENTS.md                — Project layout & rules (also loaded by other agents)
```

## Python Environment

```python
# SFX placement uses this exact sequence:
RESOLVE_LIB = r"C:\Program Files\Blackmagic Design\DaVinci Resolve\fusionscript.dll"
RESOLVE_MODULES = r"C:\ProgramData\Blackmagic Design\DaVinci Resolve\Support\Developer\Scripting\Modules"
sys.path.insert(0, RESOLVE_MODULES)
if os.path.isfile(RESOLVE_LIB):
    _d = os.path.dirname(RESOLVE_LIB)
    if not os.environ.get("PYTHONHOME"):
        os.environ["PYTHONHOME"] = sys.base_prefix
    os.environ["PATH"] = _d + os.pathsep + os.environ.get("PATH", "")
    if hasattr(os, "add_dll_directory"):
        try: os.add_dll_directory(_d)
        except OSError: pass
```

Use `davinci-resolve-mcp/venv/Scripts/python.exe` for all Python scripts.

## SFX Source Data

**Subtitle source:** `C:\Users\warit\AppData\Local\hermes\attachments\Subtitle 1.srt`
- This is the PRIMARY subtitle source — read from SRT file, NOT from DaVinci Resolve subtitle track
- Timestamps in the SRT file match the timeline exactly

## SFX Rules

- Use ONLY files in `C:\Users\warit\Desktop\davinci-katy-mcp\SFX` — never guess filenames
- SFX placed on Track 2 (SFX 1)
- Density: ~10-15/min (user preference)
- SFX ซ้อน <1s = fail; comedy sequences (fail→reaction) ติดกัน OK
- Always: dry-run → place → verify
- Every SFX must have a `reason` 1 บรรทัด

## Context Compression Awareness

When context is compressed, this `.hermes.md` is the authoritative source for:
- Which skill to invoke for each task type
- The mandatory pre-work checklist
- The SRT subtitle source path
- Python environment setup for Resolve

Do not rely on compressed context summaries — reload from this file.
